node_modules/

# Workflow outputs. These are uploaded as run artifacts rather than committed.
socket-fix-*.json
socket-fix.log
socket-patch-*.json
remediation-plan.md
pr-body.md

# Lock held while patches are being applied.
.socket/apply.lock

# Note: socket-patches.openvex.json and .socket/vendor/redirect-state.json are
# deliberately NOT ignored. The state file records the original lockfile values
# and is what `socket-patch vex` reads, so the workflow commits both.

.DS_Store
*.tgz
